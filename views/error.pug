extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Home
    center
        h1 Ruh-roh! Something bad happened
        br
        h4= error.message
        br
        h4= error.status
    br
    pre= error.stack